name: amg
channels:
  - conda-forge
dependencies:
  - python
  - gdal
  - jsonschema
  - pvl
  - psycopg2
  - pyproj
  - pystac >= 1.0.0
  - pyyaml
  - shapely
  - sqlalchemy >= 1.4, < 1.5
  - pip
  - yaml
  - pip:
      - gis-metadata-parser >= 1.2.6
