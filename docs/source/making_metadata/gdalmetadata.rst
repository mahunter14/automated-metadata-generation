GDAL Metadata
=============

The GDAL metadata container is quite straight forward. GDAL has access to pixel values and projection information. This dasta container exposes the spatial reference system (SRS) and attributes about the input data set layers.